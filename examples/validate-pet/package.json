{
  "name": "validate-pet",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "vitest run",
    "clean": "rimraf coverage build tmp",
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "axios": "^1.7.9",
    "json-p3": "^1.3.5",
    "seer-interfaces": "file:../../src/"
  },
  "devDependencies": {
    "typescript": "~5.5"
  }
}
